---
description: 每日日誌流程。當使用者說「寫今日日誌」「為一整天做結尾」「生成今天日誌」時，讀 notes/DAILY_LOG_PROCEDURE.md 並依其產出 notes/{YYYY-MM-DD}-{描述}.md（前提～訓練～評估～測試～觀察～分析～下一步～今日結論）；實驗前須先跑 camera inventory，前提要含各 camera 日夜比。
globs: notes/*.md, notes/DAILY_LOG_PROCEDURE.md
alwaysApply: false
---

# 每日日誌（Daily Log）

當使用者要求**寫今日日誌**、**為一整天做結尾**、**生成今天日誌**時：

1. **讀取** `notes/DAILY_LOG_PROCEDURE.md`，依其流程執行。
2. **檔名**：`notes/{YYYY-MM-DD}-{描述}.md`，日期以使用者提供為準，`-` 後由 AI 依當日工作命名（kebab-case）。
3. **結構**：前提（含 camera_inventory 各 camera 張數、night_ratio、mean_brightness）→ 設定 → 訓練／Val（含 FP、FN、val_best_worst_metrics.json）→ Test（含 test_best_worst_metrics.json）→ 觀察與分析 → 下一步改善方向 → 輸出檔與指令 → 今日結論。過程中點出觀察到的問題。
4. **實驗前**：先執行 `python scripts/camera_inventory.py`，日誌前提須從 `outputs/camera_inventory.csv` 整理各 camera 日夜比，以便後續回推分析。

Overlay 檔名使用 **split 編碼**（val_0042、test_0073）對照原圖；Val / Test 的 best／worst 5 另存於 `val_best_worst_metrics.json`、`test_best_worst_metrics.json`。
