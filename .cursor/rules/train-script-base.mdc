---
description: 訓練腳本以 train_multi_camera.py 為基底，保留「中斷可續訓」邏輯
globs: train*.py,**/train*.py
alwaysApply: false
---

# 訓練腳本基底：train_multi_camera.py

本專案以 **`train_multi_camera.py`** 為訓練腳本基底。撰寫或修改訓練腳本時，請以此為範本，並**務必保留「中斷可續訓」**的設計。

## 為何以此為基底

- 中斷後重新執行會**自動從上次進度繼續**，不需手動指定 checkpoint
- 每 epoch 寫入 `latest.pth`、依 val IoU 寫 `best.pth`
- 訓練日誌 `training_log.csv` 用來判斷已完成幾個 epoch，以決定 `start_epoch`

## 實驗前：Camera Inventory 先行

- 在 **split 與訓練之前**，先執行 `python scripts/camera_inventory.py`，產出 `outputs/camera_inventory.csv`。
- 撰寫 **md 日誌**時，於「前提」納入各 camera 的張數、**night_ratio**、**mean_brightness**，後續觀察訓練／測試時才能回推分析（如夜晚多、亮度低 → 易漏檢、domain shift）。

## 必須保留的「續訓」邏輯

1. **尋找未完成回合的輸出目錄**  
   - 掃描 `outputs/train_multi_camera_*`（或你的 `outputs/<run_prefix>_*`）
   - 取**最新修改時間**的目錄

2. **判斷是否續訓**  
   - 若 `training_log.csv` 存在：`completed_epochs = (行數 - 1)`（扣掉 header）
   - 若 `completed_epochs < epochs` → 使用該目錄續訓；否則開新目錄

3. **續訓時的初始化**  
   - `start_epoch = completed_epochs + 1`
   - `best_val_iou` 從既有 CSV 的 `val_iou` 取 max
   - 若有 `checkpoints/latest.pth`：載入 `model_state_dict`、`optimizer_state_dict`，再從 `start_epoch` 開始

4. **每 epoch 結束**  
   - 將該 epoch 的 metrics 追加寫入 `training_log.csv`（不要覆寫）；新 runs 的 CSV 含 **val_fp_rate、val_fn_rate**
   - 每 epoch 總結要印出 **Val FP Rate、Val FN Rate**；訓練完成時總結要印出最後 Epoch 的 Val FP/FN
   - 儲存 `latest.pth`（含 `epoch`, `model_state_dict`, `optimizer_state_dict`, `val_iou`）
   - 若 `val_iou > best_val_iou`，另存 `best.pth`

5. **訓練結束後：Val Overlay 與 best/worst 5 數據**  
   - 用 **val set** 做預測，依每張圖的 IoU 排序；使用 **best.pth** 載入模型後再產生
   - 畫 overlay：**最好 5 張、最差 5 張** 存到 `val_overlays/`；檔名使用 **split 編碼**（如 `best_1_iou_0.96_val_0042.png`），對照 **val 清單第 42 筆**即為原圖
   - **除 overlay 外**，寫入 `val_overlays/val_best_worst_metrics.json`：best_5、worst_5 每筆含 rank、source_id、iou、fp_rate、fn_rate、orig_path

6. **訓練完成後：Test 評估**（見 `notes/eval_test_procedure.md`）  
   - 執行 `eval_multi_camera_test.py`，用 **best.pth** 跑 **Test set**
   - 輸出：Test IoU、Dice、Pixel Acc、**FP Rate、FN Rate**、Loss
   - 輸出 overlay 至 `{run_dir}/test_overlays/`，檔名使用 **split 編碼**（如 `test_0073`）以對照原圖；並寫入 **test_best_worst_metrics.json**（best/worst 5 的 IoU、FP、FN、orig_path）
   - 終端印出最差 5 張的 FP Rate、FN Rate
   - 日誌需記錄：前提（Train/Val/Test 張數、各 camera 日夜比）、Val/Test 結果、失敗情境（白天/傍晚/夜晚/黃昏、灰雲、domain shift、極端失敗）

## 可依需求調整的部分

- 資料來源（splits JSON、dataset、dataloader）
- 模型、loss、optimizer、scheduler
- `epochs`、`batch_size`、`image_size`、`learning_rate`
- 是否做 AMP、overlay 張數/樣式、其它評估

## 參考程式碼位置（train_multi_camera.py）

- 續訓 / 新 run 選擇：約 300–335 行（`use_existing`、`run_dir`、`start_epoch`、`best_val_iou`、載入 `latest.pth`）
- 每 epoch 寫入 CSV：約 410–421 行
- `save_checkpoint`（`latest.pth` / `best.pth`）：約 276–294 行
- Val overlay：`create_overlay`、`evaluate_with_predictions`、`save_val_overlays`（約 191–273 行）；訓練結束後呼叫（約 420–432 行）
- Test 評估：`eval_multi_camera_test.py`；流程與記錄要點見 `notes/eval_test_procedure.md`
