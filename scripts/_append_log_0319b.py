import os
_root = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
notes_path = os.path.join(_root, "notes", "2026-03-19-模型訓練機制與Loss函數面試準備.md")

appendix = """
---

## 六、口語介紹模型的修正版本

### 原始版本（有小錯誤）

> 「我使用Meta提出的DINOv2，它看過海量的資料在沒有任何Label下做自監督學習，透過老師-學生的訓練架構，隨機遮住的學生Patch，強迫學生畫出的圖要往老師的靠攏，因此它能理解影像中細微的物理結構跟一致性，有見微知著的能力，在不微調的情況下可以應用多種任務。我用它接上卷積Decoder，第一層用來降維，第二、三層使用3*3的kernel 將特徵還原成邊界，最後一層用1x1的卷積來得到By pixel的天空機率。成為專案中表現最好的模型」

### 錯誤說明

| 原句 | 問題 | 正確說法 |
|---|---|---|
| 「隨機遮住的學生 Patch，強迫學生畫出的圖要往老師靠攏」 | 這是 MAE 的做法。DINO 是對齊**特徵分布**，不是「畫出圖」 | 「把同一張圖裁出不同的局部視角，強迫學生的特徵輸出和老師對齊」 |
| 「將特徵還原成邊界」 | 3×3 conv 是空間特徵萃取，邊界是 upsample 後的結果 | 「用 3×3 卷積萃取空間結構」 |
| 「得到 by pixel 的天空機率」 | 最後一層輸出是 logit，要接 Sigmoid 才是機率 | 「輸出每個 pixel 的分數，再經 Sigmoid 轉成天空機率」 |

### 修正後完整版（建議口語）

> 「我使用 Meta 提出的 DINOv2，它在沒有任何標注的情況下做自監督學習，透過老師-學生的訓練架構——把同一張圖裁出不同的局部視角，分別送給老師和學生，強迫學生的特徵輸出要和老師對齊。因此它能理解影像中細微的物理結構與一致性，在不 fine-tune 的情況下可以應用於多種任務。
>
> 我把 DINOv2 的特徵接上自己設計的 CNN Decoder：第一層用 1×1 卷積做通道降維，第二、三層用 3×3 卷積萃取空間結構，最後一層用 1×1 卷積輸出每個 pixel 的分數，再經 Sigmoid 轉成天空機率。整體凍結 DINOv2 的權重，只訓練 Decoder，成為這個專案中表現最好的模型。」
"""

with open(notes_path, 'a', encoding='utf-8') as f:
    f.write(appendix)
print(f"已補充至：{notes_path}")
